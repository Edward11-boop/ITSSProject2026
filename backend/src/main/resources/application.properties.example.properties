spring.application.name=book-your-seat
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.kejfeww.mongodb.net/bookyourseat?retryWrites=true&w=majority&appName=Cluster0}

spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost:5432/bookit}
spring.datasource.username=${POSTGRES_USERNAME:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:}

spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=never

google.maps.api.key=${GOOGLE_MAPS_API_KEY:}
google.maps.routes.url=https://routes.googleapis.com/directions/v2:computeRoutes
openai.api.key=${OPENAI_API_KEY:}
office.latitude=44.451311
office.longitude=26.057355
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS_ENABLE:true}

n8n.webhook.url=${N8N_WEBHOOK_URL:}
n8n.reservation-verification.webhook.url=${N8N_RESERVATION_VERIFICATION_WEBHOOK_URL:${n8n.webhook.url:}}
n8n.reminder.webhook.url=${N8N_REMINDER_WEBHOOK_URL:}
n8n.manager.approval.webhook.url=${N8N_MANAGER_APPROVAL_WEBHOOK_URL:}
n8n.invitation.webhook.url=${N8N_INVITATION_WEBHOOK_URL:}
n8n.invitation-response.webhook.url=${N8N_INVITATION_RESPONSE_WEBHOOK_URL:}
n8n.forgot-password.webhook.url=${N8N_FORGOT_PASSWORD_WEBHOOK_URL:}
